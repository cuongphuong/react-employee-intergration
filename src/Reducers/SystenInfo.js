var obj = {
    domain: 'http://localhost:8080',
    client: 'http://localhost:3000'
}

var SystemInfo = (state = obj, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

module.exports = SystemInfo;